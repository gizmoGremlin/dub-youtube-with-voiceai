# Voice.ai API Key — required for real TTS rendering.
# Get yours at https://voice.ai/dashboard (or use --mock to skip).
VOICE_AI_API_KEY=

# Optional: override the API base URL (defaults to https://dev.voice.ai)
# VOICEAI_API_BASE=https://dev.voice.ai
